"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.buildSnapGraph = buildSnapGraph;

function _cleargraph() {
  const data = require("cleargraph");

  _cleargraph = function () {
    return data;
  };

  return data;
}

function buildSnapGraph(componentLog) {
  const graph = new (_cleargraph().Graph)(); // add nodes

  componentLog.forEach(snap => {
    graph.setNode(snap.hash, snap);
  }); // add edges

  componentLog.forEach(snap => {
    snap.parents.forEach(parent => {
      graph.setEdge(snap.hash, parent, 'parent');
    });
  });
  return graph;
}

//# sourceMappingURL=snap-graph.js.map