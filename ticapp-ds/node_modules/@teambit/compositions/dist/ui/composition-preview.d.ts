/// <reference types="react" />
import { ComponentPreviewProps } from '@teambit/preview.ui.component-preview';
import { Composition } from '../composition';
interface ComponentCompositionProps extends ComponentPreviewProps {
    /**
     * composition to use for component rendering.
     */
    composition?: Composition;
}
export declare function ComponentComposition({ composition, queryParams, ...rest }: ComponentCompositionProps): JSX.Element;
export {};
