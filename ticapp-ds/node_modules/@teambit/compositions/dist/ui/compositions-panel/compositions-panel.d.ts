import React from 'react';
import { Composition } from '../../composition';
export declare type CompositionsPanelProps = {
    compositions: Composition[];
    onSelectComposition: (composition: Composition) => void;
    active?: Composition;
    url: string;
} & React.HTMLAttributes<HTMLUListElement>;
export declare function CompositionsPanel({ url, compositions, onSelectComposition: onSelect, active, className, ...rest }: CompositionsPanelProps): JSX.Element;
