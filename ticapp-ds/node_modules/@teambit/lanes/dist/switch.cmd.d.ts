import { Command, CommandOptions } from '@teambit/cli';
import { MergeStrategy } from '@teambit/legacy/dist/consumer/versions-ops/merge-version';
export declare class SwitchCmd implements Command {
    name: string;
    description: string;
    private: boolean;
    alias: string;
    options: CommandOptions;
    loader: boolean;
    report([lane]: [string], { as, merge, getAll, verbose, json, }: {
        as?: string;
        merge?: MergeStrategy;
        getAll?: boolean;
        verbose?: boolean;
        override?: boolean;
        json?: boolean;
    }): Promise<string>;
}
