import { Command, CommandOptions } from '@teambit/cli';
import { ApplyVersionResults } from '@teambit/legacy/dist/consumer/versions-ops/merge-version';
import { MergingMain } from './merging.main.runtime';
export declare class MergeCmd implements Command {
    private merging;
    name: string;
    shortDescription: string;
    group: string;
    description: string;
    alias: string;
    options: CommandOptions;
    loader: boolean;
    constructor(merging: MergingMain);
    report([values]: [string[]], { ours, theirs, manual, abort, resolve, build, noSnap, message, }: {
        ours?: boolean;
        theirs?: boolean;
        manual?: boolean;
        abort?: boolean;
        resolve?: boolean;
        build?: boolean;
        noSnap?: boolean;
        message: string;
    }): Promise<string>;
}
export declare function mergeReport({ components, failedComponents, version, mergeSnapResults }: ApplyVersionResults): string;
