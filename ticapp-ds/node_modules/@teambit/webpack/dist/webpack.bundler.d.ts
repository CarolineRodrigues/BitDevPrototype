import type { Bundler, BundlerResult, Target, BundlerContextMetaData } from '@teambit/bundler';
import type { Logger } from '@teambit/logger';
import type { Configuration } from 'webpack';
export declare class WebpackBundler implements Bundler {
    /**
     * targets to bundle.
     */
    private targets;
    /**
     * webpack configuration.
     */
    private configs;
    private logger;
    private metaData;
    private webpack;
    constructor(
    /**
     * targets to bundle.
     */
    targets: Target[], 
    /**
     * webpack configuration.
     */
    configs: Configuration[], logger: Logger, metaData: BundlerContextMetaData | undefined, webpack: any);
    run(): Promise<BundlerResult[]>;
    private getErrors;
    private getAssets;
    private getEntriesAssetsMap;
    private getCompressedSize;
    private getComponents;
}
