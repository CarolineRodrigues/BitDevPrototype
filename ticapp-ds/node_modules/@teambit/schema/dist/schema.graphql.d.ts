import { ComponentFactory } from '@teambit/component';
import { SchemaMain } from './schema.main.runtime';
export declare function schemaSchema(schema: SchemaMain): {
    typeDefs: import("graphql").DocumentNode;
    resolvers: {
        ComponentHost: {
            getSchema: (host: ComponentFactory, { id }: {
                id: string;
            }) => Promise<import("@teambit/semantics.entities.semantic-schema").SemanticSchema>;
        };
    };
};
