import { Command, CommandOptions } from '@teambit/cli';
import { ScopeMain } from '@teambit/scope';
import { Logger } from '@teambit/logger';
import { SignMain } from './sign.main.runtime';
declare type SignOptions = {
    multiple: boolean;
    alwaysSucceed: boolean;
    push: boolean;
};
export declare class SignCmd implements Command {
    private signMain;
    private scope;
    private logger;
    name: string;
    private: boolean;
    description: string;
    alias: string;
    group: string;
    options: CommandOptions;
    constructor(signMain: SignMain, scope: ScopeMain, logger: Logger);
    report([components]: [string[]], { multiple, alwaysSucceed, push }: SignOptions): Promise<string | {
        data: string;
        code: number;
    }>;
}
export {};
