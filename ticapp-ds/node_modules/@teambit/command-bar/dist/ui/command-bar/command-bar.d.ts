/// <reference types="react" />
import { CardProps } from '@teambit/base-ui.surfaces.card';
import type { CommandBarUI } from '../../command-bar.ui.runtime';
import { CommanderSearchResult } from '../../types';
export declare type CommandBarProps = {
    search: (term: string, limit?: number) => CommanderSearchResult[];
    commander: CommandBarUI;
} & CardProps;
export declare function CommandBar({ search, commander, elevation, className, ...rest }: CommandBarProps): JSX.Element;
