import { ComponentModel } from '@teambit/component';
import { SearchProvider, CommanderSearchResult } from '../../types';
declare type ComponentSearchResult = CommanderSearchResult & {
    name: string;
};
export declare class ComponentSearcher implements SearchProvider {
    private navigate;
    private fuseCommands;
    constructor(navigate: (path: string) => void);
    update: (components: ComponentModel[]) => void;
    search(term: string, limit: number): ComponentSearchResult[];
    test(term: string): boolean;
}
export {};
