import { Request, Response, NextFunction } from 'express';
import type { Logger } from '@teambit/logger';
declare type ssrRenderProps = {
    root: string;
    port: number;
    title: string;
    logger: Logger;
};
export declare function createSsrMiddleware({ root, port, title, logger }: ssrRenderProps): Promise<((req: Request, res: Response, next: NextFunction) => Promise<void>) | undefined>;
export {};
