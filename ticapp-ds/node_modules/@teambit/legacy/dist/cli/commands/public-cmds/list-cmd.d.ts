import { ListScopeResult } from '../../../consumer/component/components-list';
import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class List implements LegacyCommand {
    name: string;
    shortDescription: string;
    group: Group;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    skipWorkspace: boolean;
    remoteOp: boolean;
    action([scopeName]: string[], { ids, scope, bare, raw, json, outdated, namespace, }: {
        ids?: boolean;
        scope?: boolean;
        bare?: boolean;
        raw?: boolean;
        json?: boolean;
        outdated?: boolean;
        namespace?: string;
    }): Promise<any>;
    report({ listScopeResults, scope, ids, raw, json, outdated, }: {
        listScopeResults: ListScopeResult[];
        scope: string | null | undefined;
        ids?: boolean;
        raw?: boolean;
        json?: boolean;
        outdated?: boolean;
    }): string;
}
