import Component from '../../../consumer/component';
import { ImportDetails } from '../../../consumer/component-ops/import-components';
import { MergeStrategy } from '../../../consumer/versions-ops/merge-version/merge-version';
import { ComponentWithDependencies } from '../../../scope';
import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Import implements LegacyCommand {
    name: string;
    shortDescription: string;
    group: Group;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    remoteOp: boolean;
    action([ids]: [string[]], { tester, compiler, path, objects, displayDependencies, environment, override, verbose, json, ignoreDist, conf, skipNpmInstall, ignorePackageJson, merge, skipLane, dependencies, dependents, allHistory, }: {
        tester?: boolean;
        compiler?: boolean;
        path?: string;
        objects?: boolean;
        displayDependencies?: boolean;
        environment?: boolean;
        override?: boolean;
        verbose?: boolean;
        json?: boolean;
        ignoreDist?: boolean;
        conf?: string;
        skipNpmInstall?: boolean;
        ignorePackageJson?: boolean;
        merge?: MergeStrategy;
        skipLane?: boolean;
        dependencies?: boolean;
        dependents?: boolean;
        allHistory?: boolean;
    }, packageManagerArgs: string[]): Promise<any>;
    report({ dependencies, envComponents, importDetails, warnings, displayDependencies, json, }: {
        dependencies?: ComponentWithDependencies[];
        envComponents?: Component[];
        importDetails: ImportDetails[];
        warnings?: {
            notInPackageJson: [];
            notInNodeModules: [];
            notInBoth: [];
        };
        displayDependencies: boolean;
        json: boolean;
    }): string;
}
