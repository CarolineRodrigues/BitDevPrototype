import { ImportDetails } from '../../../consumer/component-ops/import-components';
import { ComponentWithDependencies } from '../../../scope';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Fetch implements LegacyCommand {
    name: string;
    shortDescription: string;
    description: string;
    alias: string;
    private: boolean;
    opts: CommandOptions;
    loader: boolean;
    action([ids]: [string[]], { lanes, components, json, fromOriginalScope, }: {
        lanes?: boolean;
        components?: boolean;
        json?: boolean;
        fromOriginalScope?: boolean;
    }): Promise<{}>;
    report({ dependencies, importDetails, json, }: {
        dependencies?: ComponentWithDependencies[];
        importDetails: ImportDetails[];
        json: boolean;
    }): string;
}
