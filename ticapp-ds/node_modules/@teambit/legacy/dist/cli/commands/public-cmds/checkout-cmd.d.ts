import { ApplyVersionResults } from '../../../consumer/versions-ops/merge-version';
import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Checkout implements LegacyCommand {
    name: string;
    shortDescription: string;
    group: Group;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    action([values]: [string[]], { interactiveMerge, ours, theirs, manual, reset, all, verbose, skipNpmInstall, skipDependencyInstallation, ignorePackageJson, conf, ignoreDist, }: {
        interactiveMerge?: boolean;
        ours?: boolean;
        theirs?: boolean;
        manual?: boolean;
        reset?: boolean;
        all?: boolean;
        verbose?: boolean;
        skipNpmInstall?: boolean;
        skipDependencyInstallation?: boolean;
        ignorePackageJson?: boolean;
        conf?: string;
        ignoreDist?: boolean;
    }): Promise<ApplyVersionResults>;
    report({ components, version, failedComponents }: ApplyVersionResults, _: any, { verbose, all }: {
        verbose: boolean;
        all: boolean;
    }): string;
}
