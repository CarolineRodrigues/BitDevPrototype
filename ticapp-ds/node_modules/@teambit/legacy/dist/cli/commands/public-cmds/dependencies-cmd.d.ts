import { DependenciesResultsDebug, DependenciesResults } from '../../../api/consumer/lib/dependencies';
import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Dependencies implements LegacyCommand {
    name: string;
    group: Group;
    description: string;
    alias: string;
    opts: CommandOptions;
    action([id]: [string], { debug }: {
        debug: boolean;
    }): Promise<any>;
    report(results: DependenciesResultsDebug | DependenciesResults): string;
    private debugReport;
    private nonDebugReport;
}
