import { untagResult } from '../../../scope/component-ops/untag-component';
import { Group } from '../../command-groups';
import { CommandOptions, LegacyCommand } from '../../legacy-command';
export default class Untag implements LegacyCommand {
    name: string;
    shortDescription: string;
    group: Group;
    description: string;
    alias: string;
    opts: CommandOptions;
    loader: boolean;
    migration: boolean;
    action([id, version]: [string, string], { all, force, soft }: {
        all?: boolean;
        force?: boolean;
        soft?: boolean;
    }): Promise<{
        results: untagResult[];
        isSoftUntag: boolean;
    }>;
    report({ results, isSoftUntag }: {
        results: untagResult[];
        isSoftUntag: boolean;
    }): string;
}
