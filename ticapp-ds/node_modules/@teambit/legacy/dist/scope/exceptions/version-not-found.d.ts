import { BitError } from '@teambit/bit-error';
export default class VersionNotFound extends BitError {
    version: string;
    componentId: string;
    onFs: boolean;
    constructor(version: string, componentId: string, onFs?: boolean);
}
