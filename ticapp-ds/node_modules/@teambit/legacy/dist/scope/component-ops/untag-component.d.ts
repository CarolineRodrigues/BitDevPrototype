import { Scope } from '..';
import { BitId } from '../../bit-id';
import { Consumer } from '../../consumer';
import { Lane } from '../models';
import ModelComponent from '../models/model-component';
export declare type untagResult = {
    id: BitId;
    versions: string[];
    component: ModelComponent;
};
/**
 * If not specified version, remove all local versions.
 */
export declare function removeLocalVersion(scope: Scope, id: BitId, lane: Lane | null, version?: string, force?: boolean): Promise<untagResult>;
export declare function removeLocalVersionsForAllComponents(consumer: Consumer, lane: Lane | null, version?: string, force?: boolean): Promise<untagResult[]>;
export declare function removeLocalVersionsForComponentsMatchedByWildcard(consumer: Consumer, lane: Lane | null, version?: string, force?: boolean, idWithWildcard?: string): Promise<untagResult[]>;
