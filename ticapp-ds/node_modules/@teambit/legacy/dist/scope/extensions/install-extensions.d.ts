import { Scope } from '..';
import { BitId } from '../../bit-id';
import ComponentWithDependencies from '../component-dependencies';
export default function installExtensions({ ids, dependentId, scope, verbose, dontPrintEnvMsg, }: {
    ids: [{
        componentId: BitId;
        type?: string;
    }];
    dependentId?: BitId;
    scope: Scope;
    verbose?: boolean;
    dontPrintEnvMsg?: boolean;
}): Promise<ComponentWithDependencies[]>;
/**
 * sync method that loads the environment/(path to environment component)
 */
export declare function isEnvironmentInstalled(scope: Scope, bitId: BitId): boolean;
