"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

require("core-js/modules/es.promise.js");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.fetchRemoteVersions = fetchRemoteVersions;
exports.getScopeRemotes = getScopeRemotes;

function _merge2() {
  const data = _interopRequireDefault(require("ramda/src/merge"));

  _merge2 = function () {
    return data;
  };

  return data;
}

function _globalRemotes() {
  const data = _interopRequireDefault(require("../global-config/global-remotes"));

  _globalRemotes = function () {
    return data;
  };

  return data;
}

function _remotes() {
  const data = require("../remotes");

  _remotes = function () {
    return data;
  };

  return data;
}

async function getScopeRemotes(scope) {
  const globalRemotes = await _globalRemotes().default.load();
  const globalObj = globalRemotes.toPlainObject();
  return _remotes().Remotes.load((0, _merge2().default)(globalObj, scope.scopeJson.remotes), scope);
}

async function fetchRemoteVersions(scope, componentIds) {
  const externals = componentIds.filter(id => !id.isLocal(scope.name));
  const remotes = await getScopeRemotes(scope);
  return remotes.latestVersions(externals, scope);
}