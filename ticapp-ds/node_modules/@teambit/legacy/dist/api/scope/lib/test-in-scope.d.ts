import SpecsResults from '../../../consumer/specs-results';
export default function testInScope({ id, save, verbose, // gets called during CI, verbose is always true
scopePath, directory, keep, }: {
    id: string;
    save?: boolean;
    verbose?: boolean;
    scopePath?: string;
    directory?: string;
    keep?: boolean;
}): Promise<SpecsResults | undefined>;
