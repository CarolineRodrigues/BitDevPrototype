"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

require("core-js/modules/es.promise.js");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _scope() {
  const data = require("../../../scope");

  _scope = function () {
    return data;
  };

  return data;
}

function _scopeComponentsImporter() {
  const data = _interopRequireDefault(require("../../../scope/component-ops/scope-components-importer"));

  _scopeComponentsImporter = function () {
    return data;
  };

  return data;
}

var modifyCIProps = async function modifyCIProps(id, ciProps, path) {
  const scope = await (0, _scope().loadScope)(path);
  const bitId = await scope.getParsedId(id);

  const scopeComponentsImporter = _scopeComponentsImporter().default.getInstance(scope);

  const component = await scopeComponentsImporter.loadComponent(bitId);
  return scope.sources.modifyCIProps({
    source: component,
    ciProps
  });
};

exports.default = modifyCIProps;