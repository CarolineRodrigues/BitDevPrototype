import { untagResult } from '../../../scope/component-ops/untag-component';
export default function unTagAction(version: string | undefined, force: boolean, soft: boolean, id?: string): Promise<{
    results: untagResult[];
    isSoftUntag: boolean;
}>;
