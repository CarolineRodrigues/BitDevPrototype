"use strict";

require("core-js/modules/es.promise.js");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _consumer() {
  const data = require("../../../consumer");

  _consumer = function () {
    return data;
  };

  return data;
}

function _installComponents() {
  const data = require("../../../consumer/component-ops/install-components");

  _installComponents = function () {
    return data;
  };

  return data;
}

var installAction = async function installAction(ids, verbose, packageManagerArgs) {
  const consumer = await (0, _consumer().loadConsumer)();
  consumer.packageManagerArgs = packageManagerArgs;
  const bitIds = ids.map(id => consumer.getParsedId(id));
  const installResults = ids.length ? await (0, _installComponents().installIds)(consumer, bitIds, verbose) : await (0, _installComponents().install)(consumer, verbose);
  await consumer.onDestroy();
  return installResults;
};

exports.default = installAction;