export default function remove({ ids, force, remote, track, deleteFiles, }: {
    ids: string[];
    force: boolean;
    remote: boolean;
    track: boolean;
    deleteFiles: boolean;
}): Promise<any>;
