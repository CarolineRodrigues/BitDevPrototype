import { ImportOptions } from '../../../consumer/component-ops/import-components';
export declare type EnvironmentOptions = {
    tester?: boolean;
    compiler?: boolean;
};
export default function importAction(importOptions: ImportOptions, packageManagerArgs: string[], environmentOptions?: EnvironmentOptions): Promise<any>;
