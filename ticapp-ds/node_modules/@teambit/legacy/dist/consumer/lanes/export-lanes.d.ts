import { Consumer } from '..';
import { BitIds } from '../../bit-id';
import { Lane } from '../../scope/models';
export declare function updateLanesAfterExport(consumer: Consumer, lane: Lane): Promise<void>;
export declare function getLaneCompIdsToExport(consumer: Consumer, includeNonStaged: boolean): Promise<{
    componentsToExport: BitIds;
    laneObject: Lane;
}>;
export declare function isUserTryingToExportLanes(consumer: Consumer): boolean;
