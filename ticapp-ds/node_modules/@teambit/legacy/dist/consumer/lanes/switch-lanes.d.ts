import { Consumer } from '..';
import { BitId } from '../../bit-id';
import { LaneComponent } from '../../scope/models/lane';
import { CheckoutProps } from '../versions-ops/checkout-version';
import { FailedComponents } from '../versions-ops/merge-version';
export declare type SwitchProps = {
    laneName: string;
    remoteScope?: string;
    ids?: BitId[];
    existingOnWorkspaceOnly: boolean;
    localLaneName?: string;
    remoteLaneScope?: string;
    remoteLaneName?: string;
    remoteLaneComponents?: LaneComponent[];
    localTrackedLane?: string;
    newLaneName?: string;
};
export default function switchLanes(consumer: Consumer, switchProps: SwitchProps, checkoutProps: CheckoutProps): Promise<{
    components: import("../versions-ops/merge-version").ApplyVersionResult[];
    failedComponents: FailedComponents[];
}>;
