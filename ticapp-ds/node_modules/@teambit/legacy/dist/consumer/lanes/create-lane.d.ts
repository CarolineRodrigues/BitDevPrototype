import { Consumer } from '..';
import Lane, { LaneComponent } from '../../scope/models/lane';
export default function createNewLane(consumer: Consumer, laneName: string, laneComponents?: LaneComponent[]): Promise<Lane>;
