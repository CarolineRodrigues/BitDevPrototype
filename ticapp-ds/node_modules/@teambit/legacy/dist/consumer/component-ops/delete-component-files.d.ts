import BitIds from '../../bit-id/bit-ids';
import Consumer from '../consumer';
export default function deleteComponentsFiles(consumer: Consumer, bitIds: BitIds, deleteFilesForAuthor: boolean): Promise<void>;
