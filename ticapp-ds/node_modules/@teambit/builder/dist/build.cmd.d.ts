import { Command, CommandOptions } from '@teambit/cli';
import { Logger } from '@teambit/logger';
import { Workspace } from '@teambit/workspace';
import { BuilderMain } from './builder.main.runtime';
declare type BuildOpts = {
    all: boolean;
    dev: boolean;
    rebuild: boolean;
    install: boolean;
    cachePackagesOnCapsulesRoot: boolean;
    reuseCapsules: boolean;
    tasks: string;
    listTasks?: string;
};
export declare class BuilderCmd implements Command {
    private builder;
    private workspace;
    private logger;
    name: string;
    description: string;
    alias: string;
    group: string;
    options: CommandOptions;
    constructor(builder: BuilderMain, workspace: Workspace, logger: Logger);
    report([userPattern]: [string], { all, dev, install, cachePackagesOnCapsulesRoot, reuseCapsules, tasks, listTasks, }: BuildOpts): Promise<string>;
    private getListTasks;
}
export {};
